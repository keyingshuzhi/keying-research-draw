name: CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  backend-quality:
    name: Backend Lint/Type/Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: Install Python 3.13.15
        run: uv python install 3.13.15

      - name: Sync backend dependencies
        run: uv sync --locked --extra test

      - name: Ruff
        run: uv run ruff check src/main.py src/cli/parser.py src/cli/config_schema.py src/version.py web/backend/app.py tests/test_cli_parser.py tests/test_loaders.py tests/test_smoke_app.py

      - name: Mypy
        run: uv run mypy

      - name: Pytest
        env:
          MPLBACKEND: Agg
          PLOT_BACKEND: Agg
          HEADLESS: "1"
        run: uv run pytest

  frontend-quality:
    name: Frontend Lint/Test/Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web/frontend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: web/frontend/package-lock.json

      - name: Sync frontend version
        run: node scripts/sync-version.mjs

      - name: Install frontend dependencies
        run: npm ci

      - name: Frontend lint
        run: npm run lint

      - name: Frontend tests
        run: npm run test

      - name: Frontend build
        run: npm run build

  smoke-cli:
    name: CLI Smoke
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: Install Python 3.13.15
        run: uv python install 3.13.15

      - name: Sync backend dependencies
        run: uv sync --locked --extra test

      - name: Render a smoke figure
        env:
          MPLBACKEND: Agg
          PLOT_BACKEND: Agg
          HEADLESS: "1"
        run: |
          uv run python -m src.main --mode demo --plot box --no-show --outdir /tmp/research-draw-smoke --save-name ci_smoke
          test -f /tmp/research-draw-smoke/ci_smoke.png
